# Server
server.port=8080

# MongoDB connection (can be overridden with MONGODB_URI environment variable)
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/movieist}

# JSON date handling
spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS=false

# Logging
logging.level.org.springframework=INFO
